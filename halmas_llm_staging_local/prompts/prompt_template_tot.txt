=== BLOCK 1: PERSONA ===
{persona_text}

=== BLOCK 2: OPERATIONAL STATE (injected at runtime) ===
CURRENT {tier_upper} STATUS:
{state_lines}

=== BLOCK 3: COORDINATOR INSTRUCTION ===
INSTRUCTION FROM COORDINATING AGENT:
  Rule triggered : {rule_id}  ({rule_label})
  Urgency level  : {urgency}
  Instruction    : {instruction_text}

=== BLOCK 4: DECISION HISTORY (last 3 decisions in this session) ===
{decision_history}

=== BLOCK 5b (ToT mode) ===
Consider three possible responses to the coordinator's instruction:

OPTION A - ACCEPT (factor=1.00):
  Execute the instruction fully as specified by the coordinator.
  Estimate the likely impact on your tier and the downstream supply chain.

OPTION B - MODIFY (factor=0.50-0.90):
  Execute the instruction at reduced intensity.
  Estimate which factor in this range is most appropriate given your
  current operational state, and the likely impact at that factor.

OPTION C - OVERRIDE (factor=0.20-0.50):
  Apply your own local policy instead of the coordinator's instruction.
  Estimate the likely impact on your tier and the downstream supply chain.

Evaluate all three options explicitly, then select the one that best
balances: (1) supply chain coordination effectiveness, (2) your local
operational constraints, and (3) the urgency of the current situation
({urgency}).

Then output ONLY valid JSON in this exact format, with no text before or after it:
{{
  "decision": "ACCEPT" | "MODIFY" | "OVERRIDE",
  "factor": <number between 0.0 and 1.0>,
  "justification": "<2-3 sentences explaining why you chose this option over the other two>"
}}
